package info.firozansari.core.base

//import io.reactivex.rxjava3.core.Observable

abstract class BaseViewModel<E : ViewEvent, S : ViewState> {
    //fun processEvent(event: Observable<I>)
    //fun states(): Observable<S>
}